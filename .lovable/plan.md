

## תיקון Hero — תמונת מייסדים וקרנף

### שינויים ב-`src/components/Hero.tsx`:

**תמונת המייסדים — הקטנה נוספת:**
- הקטנת המיכל מ-`height: '75%', width: '38%'` ל-`height: '65%', width: '32%'`
- שינוי ה-mask כדי שהדהייה למעלה תתחיל מוקדם יותר (מ-90% ל-95%) — כך הראש לא ייחתך

**הקרנף המצביע — הגדלה:**
- שינוי גובה מ-`h-[70px]` ל-`h-[110px]`
- הגדלת ה-opacity מ-`opacity-30` ל-`opacity-60` כדי שיהיה יותר בולט

